// Exercise 1
// Generate and array of numbers(1-100)
// (Hint: start with an empty array)

const array = [];

for (let i = 1; i <= 100; i++) {
    array.push(i);   //push into the empty array
   
}
console.log(array);